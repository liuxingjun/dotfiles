# vim: set filetype=gitconfig:

[core]
        safecrlf = warn
        autocrlf = input
	editor = vim
        pager = less
[credential]
        helper = cache
[log]
        date = iso-local
[help]
        autocorrect = 30
[pager]
        branch = false
        config = false
